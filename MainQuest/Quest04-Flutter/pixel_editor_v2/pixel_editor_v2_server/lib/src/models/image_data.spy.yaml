class: ImageData 
fields:
    pixels: ByteData # The pixel data of the image.
    width: int
    height: int

# ImageData model represents the entire image that will be sent to the app when it connects to the server.